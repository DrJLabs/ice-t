# Core dependencies - versions match Codex sandbox environment
pytest==8.3.4
pytest-cov==6.0.0
pytest-xdist==3.7.0
pytest-mock>=3.14.0

# Additional pytest plugins for CI compatibility
pytest-forked>=1.6.0
execnet>=2.0.2

# Code quality tools
ruff==0.8.4
mypy==1.13.0
isort>=5.13.0

# Pre-commit hooks
pre-commit>=3.6.0

# User interface and utilities
rich==13.9.4
click==8.1.7

# Data validation and processing
pydantic==2.10.3

# Web framework and HTTP clients
fastapi==0.115.6
requests==2.32.3
httpx==0.28.1

# Enhanced Context Manager dependencies (v2.0)
# Note: These are optional - the system will fallback gracefully if not available
numpy>=1.24.0
scikit-learn>=1.3.0

# ML dependencies for semantic search (optional)
# sentence-transformers>=2.2.0  # Uncomment if ML features needed
# faiss-cpu>=1.7.4              # Uncomment if semantic search needed

# Real-time monitoring dependencies (optional)
# watchdog>=3.0.0               # Uncomment for file change monitoring
# psutil>=5.9.0                 # Uncomment for system monitoring

# Quality Automation dependencies (offline only)
coverage==7.6.9
bandit==1.8.0
# safety removed - requires network access to vulnerability databases

# Property-based testing
hypothesis==6.122.1

# For type hints in older Python versions (if needed)
typing-extensions>=4.12.0

# Build tools
build>=1.0.0
